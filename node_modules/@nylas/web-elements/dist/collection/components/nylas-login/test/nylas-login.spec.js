import { newSpecPage } from "@stencil/core/testing";
import { NylasLogin } from "../nylas-login";
describe('nylas-login', () => {
    it('renders', async () => {
        const page = await newSpecPage({
            components: [NylasLogin],
            html: `<nylas-login></nylas-login>`,
        });
        expect(page.root).toMatchSnapshot();
    });
});
//# sourceMappingURL=nylas-login.spec.js.map
