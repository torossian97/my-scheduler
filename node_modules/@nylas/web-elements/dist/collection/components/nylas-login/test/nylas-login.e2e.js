import { newE2EPage } from "@stencil/core/testing";
describe('nylas-login', () => {
    it('renders', async () => {
        const page = await newE2EPage();
        await page.setContent('<nylas-login></nylas-login>');
        const element = await page.find('nylas-login');
        expect(element).toHaveClass('hydrated');
    });
});
//# sourceMappingURL=nylas-login.e2e.js.map
