import { newSpecPage } from "@stencil/core/testing";
import { NylasListFolders } from "../nylas-list-folders";
describe('nylas-list-folders', () => {
    it('renders', async () => {
        const page = await newSpecPage({
            components: [NylasListFolders],
            html: `<nylas-list-folders></nylas-list-folders>`,
        });
        expect(page.root).toMatchSnapshot();
    });
});
//# sourceMappingURL=nylas-list-folders.spec.js.map
