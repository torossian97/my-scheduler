import { newE2EPage } from "@stencil/core/testing";
describe('nylas-list-folders', () => {
    it('renders', async () => {
        const page = await newE2EPage();
        await page.setContent('<nylas-list-folders></nylas-list-folders>');
        const element = await page.find('nylas-list-folders');
        expect(element).toHaveClass('hydrated');
    });
});
//# sourceMappingURL=nylas-list-folders.e2e.js.map
