import { newSpecPage } from "@stencil/core/testing";
import { NylasThreadsRefresh } from "../nylas-threads-refresh";
describe('nylas-threads-refresh', () => {
    it('renders', async () => {
        const page = await newSpecPage({
            components: [NylasThreadsRefresh],
            html: `<nylas-threads-refresh></nylas-threads-refresh>`,
        });
        expect(page.root).toMatchSnapshot();
    });
});
//# sourceMappingURL=nylas-threads-refresh.spec.js.map
