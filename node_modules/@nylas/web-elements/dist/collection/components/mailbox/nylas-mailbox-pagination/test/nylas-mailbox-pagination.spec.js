import { newSpecPage } from "@stencil/core/testing";
import { NylasMailboxPagination } from "../nylas-mailbox-pagination";
describe('nylas-pagination', () => {
    it('renders', async () => {
        const page = await newSpecPage({
            components: [NylasMailboxPagination],
            html: `<nylas-mailbox-pagination></nylas-mailbox-pagination>`,
        });
        expect(page.root).toMatchSnapshot();
    });
});
//# sourceMappingURL=nylas-mailbox-pagination.spec.js.map
