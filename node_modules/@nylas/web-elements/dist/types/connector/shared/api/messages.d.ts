import { Message, NylasAuthType } from '@nylas/core';
import { NylasAuthStoreType, NylasMailboxStoreType } from '../../../components';
export declare class NylasMessagesConnector {
    private nylasAuth;
    private nylasAuthStore;
    private nylasMailboxStore;
    constructor(nylasAuth: NylasAuthType, nylasAuthStore: NylasAuthStoreType, nylasMailboxStore: NylasMailboxStoreType);
    getMessages(): Promise<Message[]>;
    getThreadMessages(threadId: string): Promise<Message[]>;
    downloadMessageAttachments(message: Message): Promise<Blob>[];
    summarizeText(text: string): Promise<string>;
    textToSpeech(text: string, abortController?: AbortController): Promise<ReadableStream<Uint8Array> | null>;
}
