export type TabContentsProps = {
    id: string;
    formState: any;
    activeTab: string;
    calendars?: any[];
};
export default function TabContents({ id, formState, activeTab, calendars }: TabContentsProps): any;
