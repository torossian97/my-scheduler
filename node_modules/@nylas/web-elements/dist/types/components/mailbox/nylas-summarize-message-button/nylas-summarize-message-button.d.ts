import { Message } from '@nylas/core';
import { EventEmitter } from '../../../stencil-public-runtime';
export type SummarizeEvent = {
    playAudio: (ttsResponseBody: Promise<ReadableStream<Uint8Array> | null>) => Promise<void>;
    updateState: (state: 'loading' | 'playing' | 'stopped') => Promise<void>;
    abortController: AbortController;
    message: Message;
};
export declare class NylasSummarizeMessageButton {
    readonly message?: Message;
    readonly summarize: EventEmitter<SummarizeEvent>;
    state: 'loading' | 'playing' | 'stopped';
    private audioRef?;
    private abortController;
    constructor();
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    private onClick;
    updateState(state: 'loading' | 'playing' | 'stopped'): Promise<void>;
    playAudio(ttsResponseBody: Promise<ReadableStream<Uint8Array> | null>): Promise<void>;
    private readChunk;
    render(): any;
}
