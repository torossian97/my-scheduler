import { EventEmitter } from '../../../stencil-public-runtime';
export type PaginationEvent = {
    hasChildren: boolean;
};
export declare class NylasMailboxPagination {
    host: HTMLNylasMailboxPaginationElement;
    state: 'ready' | 'loading' | 'error';
    hasNextPage: boolean;
    hasPreviousPage: boolean;
    scrollTarget: 'container' | 'page';
    mobileOnlyInfiniteScroll: boolean;
    mobileViewPortWidth: number;
    next: EventEmitter<PaginationEvent>;
    previous: EventEmitter<PaginationEvent>;
    private containerElement?;
    constructor();
    connectedCallback(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    componentWillRender(): Promise<void>;
    disconnectedCallback(): void;
    handlePageScroll(_event: Event): void;
    nextPage(): Promise<void>;
    previousPage(): Promise<void>;
    private handleContainerScroll;
    private isScrollAtContainerBottom;
    private isScrollAtPageBottom;
    private get canScroll();
    render(): any;
}
