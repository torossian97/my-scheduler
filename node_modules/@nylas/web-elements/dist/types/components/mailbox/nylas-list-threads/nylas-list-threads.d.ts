import { DataState } from "../../../common/types";
import { Thread } from '@nylas/core';
import { TokenInfo } from '@nylas/core/dist/auth';
import { EventEmitter } from '../../../stencil-public-runtime';
export declare class NylasListThreads {
    readonly threads: Thread[];
    readonly isAuthenticated: boolean;
    readonly tokenInfo?: TokenInfo;
    readonly state: DataState;
    readonly showCheckbox: boolean;
    readonly threadSelected: EventEmitter<Thread>;
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    private handleThreadSelected;
    private handleCheckboxClick;
    render(): any;
}
