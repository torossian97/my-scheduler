import { ObservableMap } from '@stencil/store';
export type Stores = Record<string, ObservableMap<any>>;
export type Transitions = 'none' | 'fade' | 'slide' | 'swipe';
export declare class NylasIfState {
    readonly state?: string;
    readonly isStateSet?: boolean;
    readonly isBoolean?: boolean;
    readonly getStore?: <K extends keyof Stores>(name: K) => Stores[K];
    readonly transition: Transitions;
    show: boolean;
    classes: Record<string, boolean>;
    constructor();
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    private shouldRender;
    onGetStoreChange(): void;
    private watchStore;
    private initTransition;
    render(): any;
}
