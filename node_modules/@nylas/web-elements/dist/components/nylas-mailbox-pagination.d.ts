import type { Components, JSX } from "../types/components";

interface NylasMailboxPagination extends Components.NylasMailboxPagination, HTMLElement {}
export const NylasMailboxPagination: {
    prototype: NylasMailboxPagination;
    new (): NylasMailboxPagination;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
