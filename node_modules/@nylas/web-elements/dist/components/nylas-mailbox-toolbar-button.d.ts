import type { Components, JSX } from "../types/components";

interface NylasMailboxToolbarButton extends Components.NylasMailboxToolbarButton, HTMLElement {}
export const NylasMailboxToolbarButton: {
    prototype: NylasMailboxToolbarButton;
    new (): NylasMailboxToolbarButton;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
