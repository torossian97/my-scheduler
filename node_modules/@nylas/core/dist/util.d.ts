/**
 * Function to create a SHA256 hash of a string and returns it as a base64 encoded string
 * @param str A string to hash
 * @returns A base64 encoded string with padding removed
 */
export declare function createSHA256Hash(str: string): Promise<string>;
