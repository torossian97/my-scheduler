import { IDToken, Store } from "../@types";
export declare class Storage {
    private Storage;
    constructor(store?: Store);
    setPKCE(value: string): void;
    getPKCE(): Promise<string | null>;
    removePKCE(): void;
    setGrant(resp: any, multiAccount?: boolean): void;
    removeGrant(id: string, multiAccount?: boolean): void;
    getGrant(id: string, multiAccount?: boolean): Promise<any | null>;
    setUserToken(token: IDToken): Promise<void>;
    getUserToken(id?: string): Promise<IDToken | null>;
    getUserTokens(): Promise<IDToken[] | null>;
    removeUserTokens(): void;
    removeUserToken(id: string): Promise<void>;
    clearSession(): Promise<void>;
}
